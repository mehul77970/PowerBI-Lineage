{
  "name": "powerbi-lineage",
  "version": "0.11.3",
  "description": "Generate documentation from Power BI PBIP projects — runs in your browser (nothing uploaded) or as a local CLI. Outputs a searchable dashboard plus 9+ Markdown docs ready for ADO Wiki or GitHub.",
  "license": "MIT",
  "author": "Jonathan Papworth",
  "repository": {
    "type": "git",
    "url": "https://github.com/jonathan-pap/PowerBI-Lineage.git"
  },
  "type": "module",
  "bin": {
    "powerbi-lineage": "dist/app.js"
  },
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/app.js",
    "dev": "tsc && node dist/app.js",
    "typecheck": "tsc --noEmit",
    "test": "tsc -p tsconfig.test.json && node scripts/run-tests.mjs",
    "build:browser": "tsc && tsc -p tsconfig.browser.json && node scripts/build-browser.mjs && node scripts/bake-sample.mjs",
    "serve:browser": "node scripts/serve-browser.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
